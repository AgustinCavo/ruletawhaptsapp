# IDs de grupos de WhatsApp (separados por coma para múltiples grupos)
GROUP_ID=120363xxxxxxxx@g.us

# PostgreSQL (solo necesario fuera de Docker, en Docker lo setea docker-compose)
DATABASE_URL=postgres://ruleta:ruleta@localhost:5432/cavopoints
